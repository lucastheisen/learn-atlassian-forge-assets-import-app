{
  "name": "jira-service-management-assets-import-type-ui-kit",
  "version": "1.1.37",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./dist",
    "coverage": "vitest run --coverage",
    "generate:assets-api": "npm --prefix scripts run generate:assets-api",
    "generate:assets-schema-and-mapping": "npm --prefix scripts run generate:assets-schema-and-mapping",
    "generate:secret-key": "npm --prefix scripts run generate:secret-key",
    "lint": "biome lint && forge lint",
    "smoke:upload": "npm --prefix scripts run smoke:upload",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@types/node": "^25.8.0",
    "@types/react": "^18.3.23",
    "tsx": "^4.22.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@forge/api": "^7.1.3",
    "@forge/bridge": "^5.15.1",
    "@forge/events": "^2.1.3",
    "@forge/kvs": "^1.6.4",
    "@forge/react": "^11.14.1",
    "@forge/resolver": "^1.7.1",
    "jose": "^6.2.3",
    "openapi-fetch": "^0.17.0",
    "react": "^18.2.0",
    "zod": "^3.25.76"
  }
}
